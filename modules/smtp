#Dictionary, where key is the tool name, then a list in the following order: type of command (ip, multiple), output folder, command string (DIR=output folder, HOST=ip:port, IP=ip, PORT=port, protocol

{

"smtp-user-enum": [ "ip", "smtp-user-enum", "smtp-user-enum -U /usr/share/seclists/Usernames/cirt-default-usernames.txt -t IP | tee DIR/HOST.out", "tcp"],
"ismtp-open-relay": [ "ip", "ismtp", "ismtp -h HOST -i cybertesting1@urmconsutling.com -x| tee DIR/HOST-openrelay.out", "tcp"],
"testssl-smtp": [ "ip", "testssl", "testssl -t=smtp -oJ DIR/HOST.out HOST", "tcp"],
"nmap-smtp": [ "ip", "nmap-smtp", "nmap -sV -sC -p PORT -oN DIR/HOST.out IP", "tcp"],

}
